{
    "name": "fiap-farms",
    "version": "1.0.0",
    "scripts": {
        "start": "node scripts/start.js",
        "start:docker": "node scripts/get-ip.js > .env && docker-compose up --build"
    }
}